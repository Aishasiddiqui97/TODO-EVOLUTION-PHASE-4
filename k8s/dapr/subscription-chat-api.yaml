apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: chat-api-subscriptions
  namespace: default
spec:
  pubsubname: pubsub
  topic: task-events
  routes:
    default: /events/task
  scopes:
  - chat-api
