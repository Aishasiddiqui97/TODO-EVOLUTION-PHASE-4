apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: websocket-sync-subscriptions
  namespace: default
spec:
  pubsubname: pubsub
  topic: task-updates
  routes:
    default: /events/sync
  scopes:
  - websocket-sync
