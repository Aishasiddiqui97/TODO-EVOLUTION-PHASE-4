apiVersion: v1
kind: Service
metadata:
  name: audit-log
  labels:
    app: audit-log
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8005
    protocol: TCP
    name: http
  selector:
    app: audit-log
